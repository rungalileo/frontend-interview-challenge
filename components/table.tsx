import { Text } from "@mantine/core";

const Table = ({ data, columns, onSelect }) => {
  return (
    <Text color="dimmed" sx={{ fontStyle: "italic" }}>
      
    </Text>
  );
};

export default Table;
